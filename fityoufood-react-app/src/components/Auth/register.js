import React from 'react';

const Register = () => (
    <div>
        <h2>Register Page</h2>
        <button onClick={() => alert('Register clicked!')}>Register</button>
    </div>
);

export default Register;
