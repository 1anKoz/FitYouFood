import React from 'react';

const Content = () => (
    <div>
        <h2>Home Page</h2>
        <button onClick={() => alert('Button clicked!')}>Click Me</button>
    </div>
);

export default Content;
